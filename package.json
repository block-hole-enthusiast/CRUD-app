{
  "name": "server",
  "version": "1.0.0",
  "description": "Related to CURD",
  "engines": {
    "node": "16.x"
  },
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd client && npm run build",
    "install-client": "cd client && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node index.js",
    "client": "cd client && npm start"
  },
  "author": "Raj",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "mongoose": "^4.13.21",
    "mongoose-auto-increment": "^5.0.1",
    "nodemon": "^2.0.19"
  }
}
